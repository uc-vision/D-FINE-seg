# @package train.dataset
# COCO dataset specific configuration

_target_: d_fine.dataset.coco.CocoDatasetConfig

# Dataset paths
base_path: ${train.base_path}

task: ${train.task}
examples_per_epoch: null

# Augmentations
img_config: ${train.img_config}
multiscale: ${train.multiscale}

# Note: label_to_name is derived from COCO categories, not specified here
